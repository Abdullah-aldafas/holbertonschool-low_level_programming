#include <stdio.h>
#include "main.h"
/**
 * rev_string - a function that reverses a string.
 * @s: An input integer
 * Return: Always 0
 */
void rev_string(char *s)
{
	int i;

	for (i = 0; s[i] !='\0'; i++)
	{
	}

	for (i = i - 1; i >=0; i--)
	{
		_putchar(i[s]);
	}
}
